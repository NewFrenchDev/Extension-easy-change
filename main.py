from extensionchanger import ExtensionChanger
from extensionchanger_v2 import ExtensionChangerV2

if __name__ == "__main__":

    # Create the object
    # changer = ExtensionChanger()
    changer = ExtensionChangerV2()
    # Execute the script
    changer.execute()